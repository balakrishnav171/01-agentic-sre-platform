apiVersion: v1
kind: Service
metadata:
  name: {{ include "agentic-sre.fullname" . }}
  labels:
    {{- include "agentic-sre.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "agentic-sre.selectorLabels" . | nindent 4 }}
